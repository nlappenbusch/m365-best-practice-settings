apiVersion: v1
kind: Service
metadata: { name: m365-configurator-api, labels: { app.kubernetes.io/name: m365-configurator, app.kubernetes.io/component: api } }
spec:
  type: ClusterIP
  selector: { app.kubernetes.io/name: m365-configurator, app.kubernetes.io/component: api }
  ports: [ { name: http, port: 3000, targetPort: http } ]
