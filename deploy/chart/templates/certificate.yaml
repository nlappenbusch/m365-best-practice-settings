apiVersion: cert-manager.io/v1
kind: Certificate
metadata: { name: m365-configurator-tls }
spec:
  secretName: m365-configurator-tls
  dnsNames: [ {{ .Values.host | quote }} ]
  issuerRef: { name: {{ .Values.issuer }}, kind: ClusterIssuer }
