apiVersion: v1
kind: Service
metadata: { name: m365-configurator, labels: { app.kubernetes.io/name: m365-configurator, app.kubernetes.io/component: website } }
spec:
  type: ClusterIP
  selector: { app.kubernetes.io/name: m365-configurator, app.kubernetes.io/component: website }
  ports: [ { name: http, port: 80, targetPort: http } ]
