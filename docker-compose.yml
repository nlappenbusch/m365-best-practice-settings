version: '3.8'

services:
  website:
    build: .
    container_name: m365-security-website
    restart: unless-stopped
    ports:
      - "8082:80"
    networks:
      - web

networks:
  web:
    external: false
